import { DaysLeftCountedPipe } from './days-left-counted.pipe';

describe('DaysLeftCountedPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysLeftCountedPipe();
    expect(pipe).toBeTruthy();
  });
});
